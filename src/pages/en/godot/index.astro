---
import DocPage from '../../../components/DocPage.astro';
---
<DocPage eyebrow="GODOT / OVERVIEW" title="Godot" lead="Build your UI with standard Controls, then attach the Gua adapter at the root. The adapter observes the real scene; you do not maintain a second, test-only UI model." description="Integrate Gua with Godot 4.7 using GDScript and GDExtension." section="godot">
  <div class="grid-2"><a class="mini-card" href="/en/godot/ui/"><strong>01　Implementing UI →</strong><p>Connect the automatic collector from GDScript.</p></a><a class="mini-card" href="/en/godot/testing/"><strong>02　Testing UI →</strong><p>Drive a running Godot scene from a .NET test.</p></a></div>
  <h2>GDScript + GDExtension</h2><div class="mini-card"><strong>Automatic collection</strong><p><code>native/gua-godot</code> and <code>gua_auto_adapter.gd</code> observe a broad set of standard Godot Controls.</p></div>
  <h2>Connection model</h2><div class="flow"><span>Control tree</span><b>→</b><span>Gua adapter</span><b>→</b><span>ws://127.0.0.1:8765</span><b>→</b><span>Test / Inspector / MCP</span></div>
</DocPage>
